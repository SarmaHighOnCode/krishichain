{
  "name": "@krishichain/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "KrishiChain smart contracts: actor and device registries, lot lifecycle, Merkle batch anchoring",
  "scripts": {
    "chain": "hardhat node",
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox-viem": "^3.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.17",
    "typescript": "^5.7.0",
    "viem": "^2.21.0"
  }
}

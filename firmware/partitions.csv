# KrishiChain partition table.
#
# The interesting entry is `krishibuf`: a dedicated 512 KB partition for the crash-safe
# hash-chained ring buffer (ticket H1-06). At 96 bytes per slot (90-byte canonical record +
# 6 bytes of framing/CRC) that is roughly 5,400 records — over 40 hours at a 30 s interval,
# comfortably past the 720-record / 6-hour requirement (FW-06).
#
# It lives in its own partition, not in NVS, because NVS is a key-value store with its own
# garbage collection and is the wrong shape for an append-only log we must recover by scan.
#
# Name        Type  SubType   Offset     Size      Flags
nvs,          data, nvs,      0x9000,    0x5000,
otadata,      data, ota,      0xe000,    0x2000,
app0,         app,  ota_0,    0x10000,   0x1E0000,
app1,         app,  ota_1,    0x1F0000,  0x1E0000,
krishibuf,    data, 0x40,     0x3D0000,  0x80000,
spiffs,       data, spiffs,   0x450000,  0x1A0000,
coredump,     data, coredump, 0x5F0000,  0x10000,

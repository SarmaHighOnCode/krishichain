{
  "name": "@krishichain/gateway",
  "version": "0.1.0",
  "private": true,
  "description": "Ingest, verify, batch and anchor. Verifies every record before it is ever stored.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@krishichain/core": "*",
    "aedes": "^1.1.2",
    "aedes-server-factory": "^0.2.1",
    "fastify": "^5.2.0",
    "mqtt": "^5.15.2",
    "viem": "^2.21.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}

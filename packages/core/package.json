{
  "name": "@krishichain/core",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical record encoding, signatures, hash chain, Merkle and EPCIS projection. The single source of truth shared with the firmware.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./fixtures/vectors.json": "./fixtures/vectors.json"
  },
  "scripts": {
    "test": "tsx --test src/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@noble/curves": "^1.7.0",
    "@noble/hashes": "^1.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
